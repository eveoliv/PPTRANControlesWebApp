@model IEnumerable<Models.Agenda>

    @{
        ViewData["Title"] = "Index";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }


    @section styles {
        <link href="~/Content/vendors/dataTables/css/dataTables.bootstrap.min.css" rel="stylesheet" />
    }

    <h1>Index</h1>

    <h3>Em construção</h3>

    @*<table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Agenda_Id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Data)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Cliente_id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Psicologo_Id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Medico_Id)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Agenda_Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Data)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cliente_id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Psicologo_Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Medico_Id)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
        }
            </tbody>
        </table>*@
